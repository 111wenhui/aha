1、修改mac 下hosts文件的几种方式：
找到hosts文件，可以shift+command+G 输入/private，找到etc文件夹，修改文件夹权限，erveryone修改成读和写。然后进入里面修改hosts文件。
第二种，通过终端，在终端输入 sudo vi /etc/hosts，通用linux命令方式修改保持，如果没有写的权限，可以通过sudo og+w /etc/hosts 的方式赋权。
